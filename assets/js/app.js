// Opdracht 1
const waarde = (-9);
let remainder = (waarde % 2);
//Hier word berekend met een remainder operator of de waarde even of oneven is.

console.log(`Ingevulde waarde is: ${waarde}`)
if (remainder == 0 || remainder == -0) {
    console.log("even")
} else {
    console.log("oneven")
}
// in een if else functie word gekeken of de remainder 0 of -0 is. is dat true dan is het getal even. zoniet dan is het getal oneven

//Opdracht 2
const message = 'Programming is not so cool'
console.log(message)
messagereplaced = message.replace ('not',"")
console.log(messagereplaced)
//ik vervang de not in de string met lege tekst. zodat de not uit de tekst wordt verwijderd.

//opdracht 3
const getal = (1400)
const plaats = 'ik woon in Naboo'
const vergelijking = (getal == plaats)
console.log(vergelijking)
//waarom zou je een number en string vergelijken? Het zijn twee compleet andere waardes.


